@page "/ocular-scans"

<div class="main-container">

    <!-- Left Side -->
    <div class="left-side">

        <!-- Top Part -->
        <div class="top-part">
            <button class="filter-button" onclick="zoomIn()">Zoom In</button>
            <button class="filter-button" onclick="zoomOut()">Zoom Out</button>
            <button class="filter-button" onclick="changeBrightness()">Change Brightness</button>
            <button class="filter-button" onclick="changeContrast()">Change Contrast</button>
            <button class="filter-button" onclick="adjustSaturation()">Adjust Saturation</button>
            <button class="filter-button" onclick="showTwoImages()">Show Two Images</button>
        </div>

        <!-- Bottom Part -->
        <div class="bottom-part">
            <!-- This container will hold the retinal photo(s) -->
            <img src="../wwwroot/OcularScans/DummyPatient/dummyOD.jpg" alt="Retinal Photo" />
        </div>
    </div>

    <!-- Right Side -->
    <div class="right-side">

        <!-- Upper Part - Referral Button -->
        <div class="upper-part">
            <button @onclick="ToggleReferralModal" class="referral-button">Referral</button>
            <hr class="white-line" />
        </div>

        @if (showReferralModal)
        {
            <div class="the-referral-modal"><TheReferralModal ShowTheReferralModal="ToggleReferralModal" /></div>

        }

        <!-- Lower Part - Scrollable Section with Image Squares -->
        <div class="lower-part">
            <!-- Date Squares with dates a year apart -->
            @for (int i = 0; i < 6; i++)
            {
                <div class="date-square" @onclick="() => ShowPhotoForDate(i)">
                    Date: @DateTime.Now.AddYears(-i).ToShortDateString()
                </div>
            }
        </div>

    </div>

</div>

@code {
    private bool showReferralModal = false;

    private void ToggleReferralModal()
    {
        showReferralModal = !showReferralModal;
    }

    private void ShowPhotoForDate(int yearsAgo)
    {
        DateTime selectedDate = DateTime.Now.AddYears(-yearsAgo);
    }
}
<style>
    .main-container {
        display: flex;
        height: 100vh;
    }

    .the-referral-modal {
        position: absolute;
        top: 17%;
        left: 50%;
        transform: translate(20%, -10%);
        padding: 10px;
    }

    .left-side {
        width: 70vw;
        max-height: 85vh;
        display: flex;
        flex-direction: column;
        margin-right: 2vw;
    }

    .top-part {
        width: 90%;
        height: 10%;
        padding: 10px;
    }

    .bottom-part {
        height: 88%;
        overflow: hidden;
        position: relative;
        border-radius: 5px;
        border: 1px dotted black;
        margin-top: 2vh;
    }


        .bottom-part img {
            width: 100%;
            height: 100%;
        }

    .right-side {
        width: 10%;
        max-height: 85vh;
        display: flex;
        flex-direction: column;
        background: #555;
        border-radius: 5px;
    }

    .referral-button {
        width: 95%;
        padding: 10px;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
        background-color: #3498db;
        border: none;
        color: white;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
        border-radius: 5px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        transition: box-shadow 0.4s ease;
    }

        .referral-button:hover {
            box-shadow: 0 0 20px rgba(0, 233, 233, 0.2);
        }

    .white-line {
        border-top: 1px solid white;
        width: 100%;
        margin-top: 15px;
        margin-bottom: 15px;
    }

    .upper-part {
        height: 10%;
        padding: 10px;
        margin-bottom: 3vh;
    }

    .lower-part {
        height: 90%;
        overflow-y: auto;
        padding: 10px;
    }

    .date-square {
        border: 1px solid #ddd;
        padding: 10px;
        margin-bottom: 10px;
        border-radius: 5px;
        cursor: pointer;
        background: linear-gradient(to bottom, #334d50, #cbcaa5);
    }

    .filter-button {
        background-color: #1A1A1A;
        color: white;
        border: none;
        padding: 10px;
        margin: 5px;
        cursor: pointer;
        border-radius: 5px;
        font-size: 14px;
        transition: background-color 0.4s ease;
    }

        .filter-button:hover {
            background-color: #494949;
        }

</style>
